# =============================================================================
# Azure AI Foundry Agent Toolkit - Environment Configuration
# =============================================================================
# Copy this file to .env and fill in your values:
#   cp .env.example .env
#
# IMPORTANT: Never commit .env to version control!
# =============================================================================

# -----------------------------------------------------------------------------
# Azure AI Foundry Project Endpoint (REQUIRED)
# -----------------------------------------------------------------------------
# The endpoint URL for your Azure AI Foundry project
# Format: https://<your-project-name>.services.ai.azure.com/api/projects/<project-name>
# Find this in Azure Portal > Azure AI Foundry > Your Project > Overview
PROJECT_ENDPOINT=https://your-project-name.services.ai.azure.com/api/projects/your-project-name

# -----------------------------------------------------------------------------
# Azure Authentication (OPTIONAL)
# -----------------------------------------------------------------------------
# By default, the toolkit uses DefaultAzureCredential which tries multiple
# authentication methods in order:
#   1. Environment variables (AZURE_CLIENT_ID/SECRET/TENANT_ID)
#   2. Azure CLI (az login)
#   3. Managed Identity
#   4. Azure PowerShell
#   5. Visual Studio Code credential
#
# For local development, the easiest option is to use Azure CLI:
#   az login
#   az account set --subscription "Your-Subscription-Name"
#
# For service principal authentication, set these variables:
# AZURE_CLIENT_ID=your-service-principal-client-id
# AZURE_CLIENT_SECRET=your-service-principal-client-secret
# AZURE_TENANT_ID=your-azure-tenant-id

# -----------------------------------------------------------------------------
# Optional: Azure Subscription (for reference/scripting)
# -----------------------------------------------------------------------------
# Not used directly by the toolkit, but useful for scripts
# AZURE_SUBSCRIPTION_ID=your-subscription-id
